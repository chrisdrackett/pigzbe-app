import {StyleSheet} from 'react-native';

import {
    color,
    fontFamily,
} from 'app/styles';

export default StyleSheet.create({
    address: {
        marginBottom: 30,
    },
    line: {
        fontFamily,
        color: color.blue,
        fontSize: 16,
        textAlign: 'center',
    }
});
