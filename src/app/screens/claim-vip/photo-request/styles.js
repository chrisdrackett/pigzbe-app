import {StyleSheet} from 'react-native';

import {
    color,
    fontFamily,
} from 'app/styles';

export default StyleSheet.create({
    modal: {
        flex: 1,
        backgroundColor: color.blue,
    }
});
