import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    button: {
        // backgroundColor: 'red',
        alignItems: 'center',
        justifyContent: 'center',
    },
});
