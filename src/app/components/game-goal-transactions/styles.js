import {StyleSheet} from 'react-native';

import {
    color,
    fontFamily
} from '../../styles';

export default StyleSheet.create({
    box: {
        backgroundColor: color.white,
        borderRadius: 4,
        margin: 10,
        padding: 20,
        flex: 1,
    },
});
