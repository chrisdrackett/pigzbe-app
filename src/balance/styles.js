import {StyleSheet} from 'react-native';
import {
    container,
    color,
    fontFamily
} from '../styles';

export default StyleSheet.create({
    container,
    welcome: {
        fontFamily,
        color: color.white,
        fontSize: 18
    },
    balance: {
        fontFamily,
        color: color.white,
        fontSize: 40
    },
    label: {
        fontFamily,
        color: color.white,
        fontSize: 18
    }
});
