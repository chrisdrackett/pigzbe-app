import {StyleSheet} from 'react-native';
import {
    container,
    color,
    fontFamily
} from '../styles';

export default StyleSheet.create({
    container,
    title: {
        fontFamily,
        color: color.white,
        fontSize: 18
    },
    text: {
        fontFamily,
        color: color.white,
        fontSize: 16
    }
});
